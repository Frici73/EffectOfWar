<Window x:Class="EffectOfWar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EffectOfWar"
        mc:Ignorable="d"
        Title="Effect of War" Height="450" Width="800" ResizeMode="CanMinimize" WindowState="Maximized">
    <Grid>
        <Grid x:Name="Menu"
      Grid.ColumnSpan="2"
      HorizontalAlignment="Stretch"
      MinWidth="1000">

            <!-- 5 oszlop -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.3*"/>
            </Grid.ColumnDefinitions>

            <!-- ===================== -->
            <!-- WARriors -->
            <!-- ===================== -->
            <ListBox x:Name="WarriorsList"
             Grid.Column="0"
             HorizontalAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Height" Value="120"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <Image Source="{Binding ImagePath}"
                           Stretch="UniformToFill"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <!-- ===================== -->
            <!-- RANGERS -->
            <!-- ===================== -->
            <ListBox x:Name="RangersList"
             Grid.Column="1"
             HorizontalAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Height" Value="120"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <Image Source="{Binding ImagePath}"
                           Stretch="UniformToFill"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <!-- ===================== -->
            <!-- SUPPORTS -->
            <!-- ===================== -->
            <ListBox x:Name="SupportsList"
             Grid.Column="2"
             HorizontalAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Height" Value="120"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <Image Source="{Binding ImagePath}"
                           Stretch="UniformToFill"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <!-- ===================== -->
            <!-- BOSSES -->
            <!-- ===================== -->
            <ListBox x:Name="BossesList"
             Grid.Column="3"
             HorizontalAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Height" Value="120"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <Image Source="{Binding ImagePath}"
                           Stretch="UniformToFill"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <!-- ===================== -->
            <!-- BASIC STAT BOX -->
            <!-- ===================== -->
            <TextBox x:Name="BasicStatShower"
             Grid.Column="4"
             Margin="5"
             MinWidth="250"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"/>

        </Grid>
        <Grid x:Name="Battleground" Grid.ColumnSpan="2">
            <TextBox x:Name="ConsoleD"/>

        </Grid>
    </Grid>
</Window>
